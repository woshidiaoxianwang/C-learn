//1.函数只能return一个值
//2.函数不可以嵌套定义，但可以嵌套调用，
//3.形参和实参可以同名，
//4.函数的定义（怎么实现）不一定放在使用之前，但声明必须放在使用之前，函数必须先声明再调用
//5.函数声明，就是告诉编译器这儿有个函数，它叫XXX，参数是XXX，返回值是XXX，至于函数怎么实现，未说明，
//函数定义，告诉编译器，不光它叫XXX，参数是XXX，返回值是XXX，还有实现的代码也告诉编译器了。
//6.函数调用exec((v1, v2),(v3 , v4),v5, v6);
//里面有四个参数，因为逗号表达式按最后一个算。
//7.